# FinBank Backend (Spring Boot + JWT)

- Spring Boot 3
- Spring Security + JWT
- H2 인메모리 DB
- 계좌/거래 로그, 비관적 락(PESSIMISTIC_WRITE) 기반 이체

## 실행 방법

```bash
cd finbank-backend
chmod +x gradlew   # 최초 1회
./gradlew bootRun
```
